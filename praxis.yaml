# install: conda env create -f praxis.yaml
# update:  conda env update -f praxis.yaml
name: praxis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - ipython
  - ipykernel
  - matplotlib
  - geopandas
  - pandas
  - pip:
    - rasterio


